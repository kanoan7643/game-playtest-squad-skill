interface:
  display_name: "Game Playtest Squad"
  short_description: "Run genre-aware game playtest squads."
  default_prompt: "Use $game-playtest-squad to preflight the test environment, classify the game type, run the appropriate playtest tier, and summarize issues, evidence, fixes, and retest results."
